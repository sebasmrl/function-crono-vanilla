# Funcion Cronometro
Este repositorio trata de una funcion que cronometro que recibe el tiempo del cronometro,
el selector del elementoContenedor, el nodo de el reloj, y la callback action en donde
se configura la tranformacion de el reloj cada segundo hasta que el tiempo sea cero.

La funcion internamente hace un unmount del reloj cuando llega a cero